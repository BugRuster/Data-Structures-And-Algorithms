#include <iostream>
using namespace std;

int main ()
{
    int n, m; cin >> n, m;
    int arr[n][m];
    
    for (int i = 0; i < l1; i++) {
        for (int j = 0; j < l2; j++) {
            cout << arr[i][j];
        }
        cout << endl;
    }

    int l1, l2; cin  >> l1 >> l2;
    int arr[l1][l2];
    for (int i = 0; i < l1; i++) {
        for (int j = 0; j < l2; j++) {
            cin >> arr[i][j];
    }
    }
    return 0;
}
 
 
// @BugRuster